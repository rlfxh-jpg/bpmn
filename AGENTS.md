# 开发约定

## 注释要求

- 代码需要加上必要注释，尤其是复杂逻辑、关键流程和不直观的实现部分。
- 注释应解释“为什么这样做”或“这段逻辑解决什么问题”，避免只重复代码表面含义。
- 公共模块、扩展点、注册中心、字段读写服务、BPMN 行为定制等位置应优先补充注释。

## 命名规范

- 变量、函数使用语义明确的英文命名，避免无意义缩写。
- 组件文件使用 `PascalCase` 命名，例如 `BpmnEditor.vue`。
- 普通 TypeScript 文件使用 kebab-case 或与现有目录风格保持一致。
- 常量使用全大写下划线风格，尤其是共享字段名、事件名、type 标识。
- 可插拔节点的 `key` 必须稳定、唯一、可读，例如 `approval-task`、`system-task`。

## 目录职责

- `src/components/`：通用组件或编辑器封装组件，不放具体业务节点定义。
- `src/views/`：页面级组件，负责页面编排，不承载复杂底层逻辑。
- `src/features/`：按功能域组织的业务能力与扩展能力。
- `src/features/bpmn-nodes/`：可插拔 BPMN 节点扩展系统。
- `src/features/bpmn-nodes/core/`：节点扩展框架公共内核，包括类型、注册中心、共享服务、moddle、公共模块。
- `src/features/bpmn-nodes/<node-name>/`：单个节点的独立 feature 目录，内部放该节点自己的定义、字段、行为、属性面板配置等。
- `src/features/bpmn-validation/`：保存前校验规则、上下文和规则执行器。
- `src/assets/`：静态资源，如默认 BPMN XML。
- `src/styles/`：全局样式与编辑器样式覆盖。

## 组件拆分原则

- 页面组件只负责布局、事件编排和数据流转，不直接操作过多 `bpmn-js` 细节。
- 编辑器组件负责模型器生命周期、对外暴露能力、事件桥接，不直接承担页面业务逻辑。
- 复杂逻辑优先抽到 `features` 下的模块或服务中，不要持续堆积在 `.vue` 页面中。
- 单个组件或模块职责应尽量单一，避免同时承担 UI、状态管理、协议转换和底层建模操作。
- 当某块逻辑在两个以上节点或两个以上页面会复用时，应提取为共享模块。

## 可插拔 Feature 开发要求

- 不允许脱离 BPMN 标准随意发明新的基础元素类型，优先基于标准元素扩展。
- 每个节点扩展必须放在 `src/features/bpmn-nodes/<node-name>/` 下。
- 每个节点扩展应通过统一定义对象接入注册中心，不允许页面层硬编码节点逻辑。
- 节点扩展应尽量按 `definition / fields / behavior / properties / palette / renderer` 等职责拆分。
- 扩展字段的读写必须优先复用 `core/services` 中的统一服务，不要在页面或节点内部重复拼装 XML 结构。
- 节点启用与停用应通过 preset 或统一配置控制，不要在页面里散落条件分支。
- 新增节点时，原则上不应修改页面主逻辑；如果必须修改，说明当前抽象层还不够稳定，需要先优化结构。

## 提交前校验

- 提交前至少执行一次 `pnpm exec vue-tsc --noEmit`。
- 提交前至少执行一次 `pnpm build`。
- 如果改动涉及 BPMN 扩展字段、导入导出、校验规则或节点注册，需要同时手动验证对应流程。
- 如果改动涉及 UI 交互，需至少确认页面可正常加载、关键按钮可用、无明显控制台错误。
