/**
 * 预留给审批任务专属行为扩展的文件。
 *
 * 当前节点创建后的默认行为已经由 core/create-behavior 统一处理，
 * 所以审批任务没有额外的专属行为代码。
 *
 * 后续如果审批任务需要：
 * - 创建时自动挂更多默认配置
 * - 根据上下文限制创建位置
 * - 创建后触发额外业务规则
 * 可以在这里承接，而不污染 core 通用逻辑。
 */
export {}
