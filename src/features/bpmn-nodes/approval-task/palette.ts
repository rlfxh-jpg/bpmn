/**
 * 预留给审批任务 palette 专属逻辑的文件。
 *
 * 当前左侧工具栏由 core/palette-provider 基于 definition 统一生成，
 * 因此审批任务本身不需要在这里额外实现代码。
 *
 * 保留该文件的原因：
 * - 目录结构完整，表达“每个节点理论上都可以拥有自己的 palette 扩展点”
 * - 后续若审批任务需要自定义更复杂的 palette 行为，可直接在此扩展
 */
export {}
