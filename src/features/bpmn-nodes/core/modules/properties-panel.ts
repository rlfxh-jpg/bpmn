/**
 * 预留给自定义业务属性面板模块的入口文件。
 *
 * 当前右侧业务属性面板仍由 Vue 页面层承接，
 * 这里保留文件是为了让目录结构完整，也方便后续把业务属性面板进一步下沉到 core/modules。
 *
 * 之所以先保留占位，而不是直接删除：
 * - 可以明确表达“properties panel 是节点扩展体系中的一等能力”
 * - 后续演进时无需再调整目录约定
 */
export {}
